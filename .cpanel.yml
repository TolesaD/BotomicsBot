# .cpanel.yml - Yegara.com Deployment Configuration
---
deployment:
  tasks:
    - export DEPLOYPATH=/home/maroseff/public_html/marcreator/
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - npm install --production
    - echo "✅ Deployment completed successfully"